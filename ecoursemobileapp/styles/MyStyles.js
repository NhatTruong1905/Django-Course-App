import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginTop: 60
    },
    row: {
        flexDirection: 'row',
        flexWrap: 'wrap'
    },
    margin: {
        margin: 5
    }
});

